# Server Environment Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration (Relative or Absolute Path / Connection String)
DATABASE_PATH=./student_gateway.db

# CORS Configuration (Allowed origins: comma-separated list or * for all)
CORS_ORIGIN=*
